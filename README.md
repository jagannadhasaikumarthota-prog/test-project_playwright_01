# Playwright TypeScript Sample

This project contains a clean Playwright TypeScript setup with:
- a single Google sign-in flow test
- a page object model under `pages/`
- Allure reporting outputs in `allure-results/` and `allure-report/`

## Install and run

```bash
npm install
npx playwright install chromium
npm test
npm run report:generate
```
